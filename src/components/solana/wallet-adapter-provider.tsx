/**
 * This file is kept for compatibility but wallet-ui is now used instead
 * Can be removed if not needed
 */
export function WalletAdapterProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
